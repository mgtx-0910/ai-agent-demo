# 2026-09-19

## knowledge-hub-backend 接口同步到 Apipost

- 目标项目：Apipost 单机版「默认项目」，`project_id = 2217ab9975f003`
- 同步方式：`apipost_sync_openapi`，`api_cover_mode = url_and_folder`，传入 OpenAPI 3.0.3 文档
- 结果：`created = 6`，自动按 tags 建了两个目录
  - 目录「健康检查」：`GET /`（存活探针）
  - 目录「文档管理」：`POST /documents`、`GET /documents`、`GET /documents/{id}`、`PATCH /documents/{id}`、`DELETE /documents/{id}`
- 数据模型（components.schemas）按名称导入：CreateDocumentDto、UpdateDocumentDto、Document、DocumentWithContent、DocumentListResponse、DeleteResponse、ErrorResponse
- 接口摘要来源：`src/document/document.controller.ts`、`src/app.controller.ts`、`src/document/dto/*.ts`、`src/document/entities/document.entity.ts`

## 经验记录

- Apipost MCP 的 `openapi` 参数必须是「不含任何转义字符」的 JSON：description 里不要写 `\n`，字符串内不要出现被转义的双引号，直接传缩进格式的 JSON 对象最稳；否则统一报 `arguments must be a valid JSON string`
- 给 query 参数写了 `example` 时，同步后的接口 URL 会把示例值拼进 query（如 `/documents?title=入职&categoryId=20001&...`），属正常行为，需要干净 URL 可在 Apipost 里手动清空

## 提交推送 knowledge-hub-backend 文件解析能力

- commit：`5c7a08f`，已推送 `origin/master`（f3c4430 → 5c7a08f），42 files changed
- 核心内容：新增 `POST /documents/upload/parse`（multipart，字段名 file，50MB 上限），支持 pdf/docx/xlsx/pptx/txt/md 解析为 Markdown 并创建草稿文档
- 新增模块：`src/document/parser/`（FileParserService + 5 个 parser + markdown.util）、`src/storage/`（RustfsService，S3 兼容对象存储）
- 基础设施：docker-compose 加 rustfs 服务（9000/9001）；PG 端口 15432 → 5432；.env.example 加 RUSTFS_*；新增 pnpm-workspace.yaml
- 提交处理：`tmp.md`（pptx 解析临时产物）已写入 `knowledge-hub-backend/.gitignore`，未提交
- 提交信息用中文多行文本，做法是写成 UTF-8 文件后 `git commit -F` 再删除，避免 PowerShell 传参编码问题；若在 PowerShell 里直接 `git commit -m "中文"` 有乱码风险

## 待与用户确认的两处异常

- 本次改动把 knowledge-hub-backend 内大量详细 JSDoc 精简为单行注释（app.module / main / document.service / entity / schema 等），与上一版风格相反
- `README.md` 被替换回 NestJS 默认模板（原 269 行中文项目说明被覆盖，内容仍可从 `f3c4430` 找回）
- `test-files/` 含真实简历文件（姓名 + 手机号），已随本次提交推送到 GitHub 公开仓库，需确认是否要移除
