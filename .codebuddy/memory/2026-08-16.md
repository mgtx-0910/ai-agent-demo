# 2026-08-16

## tts-stt-test 项目补注释
- 为 `tts-stt-test/src` 下三个 `.mjs` 补齐注释，遵循项目注释规范（文件头 JSDoc + `@see` 关联 + 代码内步骤/字段行尾注释）。
- 三个文件及职责：
  - `tts-test.mjs`：腾讯云一句话语音合成（TextToVoice，SDK `tencentcloud-sdk-nodejs-tts`）。
  - `asr-test.mjs`：一句话语音识别（SentenceRecognition，SDK `tencentcloud-sdk-nodejs`）。
  - `streaming-tts-test.mjs`：流式语音合成（WebSocket + TextToStreamAudioWSv2 + HMAC-SHA1 签名）。
- 修正了 `tts-test.mjs` 中 `VoiceType: 502006` 的错误注释（原注释误写为 101007 智瑜），改为通用音色描述。
- `read_lints` 无错误。

## asr-and-tts-nest-service 项目补注释
- 为 NestJS 服务 `asr-and-tts-nest-service` 全部 14 个 `.ts` 文件补齐注释（文件头 JSDoc + `@see` 关联 + 行尾/步骤注释）。
- 项目架构：AI 对话（LangChain 流式 SSE）+ 语音识别（ASR）+ 语音合成（TTS）三者联动。
  - 根层：`main.ts`（启动 + 挂载 WebSocket `/speech/tts/ws`）、`app.module.ts`、`app.controller.ts`、`app.service.ts`。
  - `ai/`：`ai.controller.ts`（SSE `/ai/chat/stream`）、`ai.module.ts`（注入 CHAT_MODEL）、`ai.service.ts`（streamChain 异步生成器）。
  - `speech/`：`speech.controller.ts`（POST `/speech/asr`）、`speech.module.ts`（注入 ASR_CLIENT）、`speech.service.ts`（SentenceRecognition）、`tts-relay.service.ts`（双 WebSocket 中继，最复杂）。
  - `common/stream-events.ts`：AI→TTS 事件定义（start/chunk/end/error）。
- 关键链路：AiService.streamChain 逐分片发事件 → 事件总线 → TtsRelayService → 腾讯云 TTS WebSocket → 音频分片转发回浏览器客户端。
- 验证：`read_lints` 无错误，`npx tsc --noEmit` 通过（exitCode 0）。

