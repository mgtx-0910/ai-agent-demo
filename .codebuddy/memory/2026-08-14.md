# 2026-08-14

## 重写 cron-job-tool README

- 按用户要求重写了 `cron-job-tool/README.md`：保留脚手架运行命令（改用 npm，项目用 package-lock.json），新增项目简介、技术栈、目录结构、核心概念（Agent/工具/ReAct/定时任务三类型）、工具清单（6 个工具表格 + 两个 Agent 绑定差异）、4 条调用流程（普通对话 ReAct / SSE 流式 / 创建定时任务 / 到点执行，均配 mermaid 时序图）、环境变量说明、接口速查。
- 梳理出的关键架构事实：
  - 两个独立 Agent：AiService（对话，绑定 6 工具：query_user/send_mail/web_search/db_users_crud/time_now/cron_job）；JobAgentService（任务，绑定 4 工具，不含 query_user 和 cron_job 以防递归）。
  - 工具通过 ToolModule 字符串 token 导出（如 'SEND_MAIL_TOOL'），AiModule/JobModule 用 @Inject 注入。
  - 定时任务三类型：cron（CronJob）/ every（setInterval）/ at（setTimeout，执行一次后停用）。启动时 JobService.onApplicationBootstrap 从 DB 恢复 isEnabled 任务。
  - 项目运行时未启用 Swagger UI（main.ts 只有 create+listen），接口文档靠 `npx ts-node src/generate-openapi.ts` 生成 openapi.json（需 MySQL 可用）。
